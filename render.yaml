services:
  - type: web
    name: bwf-portal
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false   # set manually in dashboard
      - key: JWT_SECRET
        sync: false   # set manually in dashboard
